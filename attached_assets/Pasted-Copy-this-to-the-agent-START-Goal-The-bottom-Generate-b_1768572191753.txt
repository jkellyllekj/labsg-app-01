Copy this to the agent

START

Goal
The bottom Generate button did not appear. Fix it by inserting the button INSIDE the Advanced options panel (inside advancedWrap) and wiring it to click the existing generateBtn. UI wiring only.

Hard constraints
- Edit index.js in the repo root only
- One micro change only
- Do not change generation logic, snapping, tiers, layout, or CSS
- Do not rename existing ids or classes
- Do not run tests. Manual testing will be done by the human

Step 0. Open the file
Open index.js (repo root).

Step 1. Confirm the bottom button is missing in the HTML template
Search for this exact string:
generateBtn2
If it is not found, proceed. If it is found, it is in the wrong place. We will reinsert correctly inside advancedWrap.

Step 2. Find the Advanced options wrapper
Search for this exact string:
<div id="advancedWrap"
This is the Advanced options panel.

Step 3. Find the Notes block inside advancedWrap
Within advancedWrap, find:
id="notes"
You will see the Notes textarea block.

Step 4. Insert the bottom Generate button INSIDE advancedWrap
Immediately after the Notes textarea block, but BEFORE the closing </div> that ends advancedWrap, insert this HTML:

<div style="margin-top:14px; display:flex; justify-content:flex-end;">
  <button id="generateBtn2" type="button" class="generateBox" style="width:100%; max-width:220px;">
    <div class="genLabel">Generate</div>
    <div class="genDolphin">
      <img class="dolphinIcon dolphinIcon--generate" src="/assets/dolphins/dolphin-base.png" alt="">
    </div>
  </button>
</div>

Important
- The inserted markup must be inside the <div id="advancedWrap" ...> block.
- Do not place it after advancedWrap closes.

Step 5. Wire the button in JS
Now search for:
document.getElementById("regenBtn2")
This is the existing wiring area for title-area buttons.

Directly after the existing regenBtn2 and bgCycleBtn2 wiring, add:

document.getElementById("generateBtn2")?.addEventListener("click", () => {
  document.getElementById("generateBtn")?.click();
});

Step 6. Save and commit
Save index.js.

Commit message:
Fix bottom Generate button placement in Advanced options

Step 7. Report back only
Reply with:
- line numbers for the HTML insertion (start and end lines)
- line numbers for the JS wiring insertion
- confirmation only index.js changed
- commit hash

Do not run the app or test.

FINISH
