1) Fix the effort strip sizing and centering in styles.css

Open: /mnt/data/styles.css

Find this block (it starts at the comment):

/* Effort strip wrapper: swipe only when needed */
.effortScrollWrap {
  overflow-x: auto;
  overflow-y: hidden;
  -webkit-overflow-scrolling: touch;
}

/* Effort strip: 5 equal tiles, fills big phones, scrolls on tiny */
.effortStrip {
  display: flex;
  flex-wrap: nowrap;
  width: 100%;
  min-width: 360px;
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 2px 6px rgba(0,0,0,0.2);
}

/* True square tiles + dead-centre content */
.effortTile {
  flex: 1 1 0;
  aspect-ratio: 1 / 1;
  box-sizing: border-box;
  padding: 2px;
  display: flex;
  align-items: center;
  justify-content: center;
}


Replace it with this:

/* Effort strip wrapper: swipe only when needed */
.effortScrollWrap {
  overflow-x: auto;
  overflow-y: hidden;
  -webkit-overflow-scrolling: touch;
}

/* Effort strip: fills big phones, scrolls on tiny */
.effortStrip {
  display: flex;
  flex-wrap: nowrap;
  width: 100%;
  min-width: 320px; /* was 360, this is why big phones still clipped */
  border-radius: 8px;
  overflow: hidden;
  box-shadow: 0 2px 6px rgba(0,0,0,0.2);
}

/* True square tiles + dead-centre content */
.effortTile {
  flex: 1 1 0;
  aspect-ratio: 1 / 1;
  box-sizing: border-box;
  padding: 0; /* reclaim pixels */
  display: flex;
  align-items: center;
  justify-content: center;
}

/* Bigger icons, no margin tricks */
.effortIcon {
  width: 62px;
  height: 62px;
  object-fit: contain;
  display: block;
  margin: 0;
  filter: none !important;
}

/* Micro nudges to counter uneven transparent padding in PNGs */
.effortStrip .effortTile:nth-child(2) .effortIcon { transform: translateX(3px); }  /* green: push right */
.effortStrip .effortTile:nth-child(3) .effortIcon { transform: translateX(-3px); } /* beige/yellow: pull left */
.effortStrip .effortTile:nth-child(4) .effortIcon { transform: translateX(-2px); } /* orange: slightly left */


This does three things:

Makes it fit on large phones without clipping (min-width: 320px).

Removes wasted border padding (padding: 0).

Forces “dead centre” and fixes the left or right drift with tiny per-tile nudges.

2) Stop the fading in index.js (so Threshold and Full Gas never look greyed out)

Open: /mnt/data/index.js

Find this exact snippet:

for (let i = 0; i < 5; i++) {
  const isActive = (i + 1) <= level;
  const opacity = isActive ? '1' : '0.4';

  strip += '<div class=\\"effortTile\\" style=\\"background:' + bgColors[i] + ';\\">';
  strip += '<img class=\\"effortIcon\\" src=\\"' + dolphinIcons[i] + '\\" alt=\\"' + iconAlts[i] + '\\" style=\\"opacity:' + opacity + ';\\">';
  strip += '</div>';
}


Replace it with:

for (let i = 0; i < 5; i++) {
  strip += '<div class=\\"effortTile\\" style=\\"background:' + bgColors[i] + ';\\">';
  strip += '<img class=\\"effortIcon\\" src=\\"' + dolphinIcons[i] + '\\" alt=\\"' + iconAlts[i] + '\\">';
  strip += '</div>';
}


Now:

No tile ever fades.

The icons stay blue (your CSS already enforces filter: none !important;).

“Full Gas” is obvious purely from the dolphin face, which is what you w